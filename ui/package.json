{
  "name": "pebble-engine-ui",
  "version": "1.0.0",
  "private": true,
  "description": "Build script for the Pebble Engine quiz UI's Tailwind CSS. Run `npm run build` from this directory to regenerate ui/style.css from index.html + tailwind.config.js after any class change.",
  "scripts": {
    "build": "tailwindcss -i input.css -o style.css --minify",
    "watch": "tailwindcss -i input.css -o style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/container-queries": "^0.1.1"
  }
}
