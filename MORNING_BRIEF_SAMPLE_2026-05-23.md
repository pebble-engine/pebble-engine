# 🟡 Pebble morning brief — 2026-05-23 (SAMPLE)

_Generated at 2026-05-23T14:48:00+00:00 · window: last 36h · overall severity: **warn**_

> **About this file:** This is the **enriched** sample showing what the brief looks
> like once `SENTRY_PAT` + `PEBBLE_SUPABASE_PAT` are set in `.env`. The Sentry and
> Supabase sections below were populated from live MCP queries at write time; the
> rest is the actual unmodified script output. Without those PATs, those two sections
> show a "_skipped_" stub with instructions for generating each token — every other
> section still works.
>
> **To generate yourself:** `python -m pebble.morning_brief --hours 36 --root C:/Users/marci/pebble-engine`
> Design doc: [`docs/MORNING_BRIEF.md`](docs/MORNING_BRIEF.md)

---

## 🟢 Engine errors

✓ No errors or warnings in the last log scan.

_Note: lines aren't timestamped, so the 36h window is reported as context — not used for filtering. Last 5MB of engine.err.log scanned._

## 🟢 Sentry errors

✓ 0 unresolved issues across all projects in the last 36h.

_(Marc's overnight cleanup pass removed the ~7 test events from the Sentry rollout. Two real projects monitored: `pebblev3` (frontend) + `python` (engine).)_

## 🟡 Supabase advisors

**2 security finding(s)**, **3 performance finding(s)** currently open.

By level: WARN×4, INFO×1

**Top 5 security findings:**
  - [WARN] `RLS Policy Always True` — `public.waitlist` allows anon INSERT (intentional per Phase 58e doc — waitlist signup form)
  - [WARN] `Leaked Password Protection Disabled` — Supabase Pro gate, deferred per Phase 58e doc

**Top 3 performance findings:**
  - [WARN] `Auth RLS Initialization Plan` — `public.profiles` policy "Profiles are visible to their owner" re-evaluates `auth.uid()` per row. At scale, suboptimal. Fix: wrap as `(select auth.uid())`. [docs](https://supabase.com/docs/guides/database/postgres/row-level-security#call-functions-with-select)
  - [WARN] `Auth RLS Initialization Plan` — same pattern on `public.profiles` policy "Profiles are updatable by their owner"
  - [INFO] `Unused Index` — `idx_waitlist_email` on `public.waitlist` has never been served by a query

_(Both performance WARNs are the same `(select auth.uid())` fix repeated for two policies. ~15-minute SQL migration to resolve both at once.)_

## 🟢 Code activity (git)

**44 commit(s)** in the last 36h:
    `31fff86 fix(csp): allow Google + GitHub OAuth provider origins (Phase 55.1)`
    `e55ccc8 docs(trust): Mozilla Observatory B+ verified — moved to "Verified credentials"`
    `3bce57d feat(security): full security headers for Mozilla Observatory A grade (Phase 55)`
    `2ce64a4 feat(plans): plan-picker offer wall modal (Phase 54c v1)`
    `3b824cc feat(plans): intentional Free signup — needs_plan_selection gate (Phase 54b)`
    `2a4b95e fix(plans): wire HARD_CEILINGS clamp into get_limit + drop paid CSA from /trust`
    `ff1740f feat(trust): SSL Labs A+ achieved + HSTS preload submitted + HARD_CEILINGS`
    `67fc1d4 feat(plans): backend gates for refinements, domains, blocks, analytics, email (Phase 54a)`
    _… 36 more_

**107 unpushed commit(s)** on current branch — `git push` when ready.

## 🟢 Build activity

(no builds in the last 36h)

_(Worktree's output/ has 3 builds today @ $0.0681 — those run from `bold-hopper-c3631f`, not the base repo. Run with `--root <worktree-path>` to see those.)_

## 🔵 User engagement

(no engagement events in the last 36h)

## 🔵 Subscriptions

**Plans:** starter×2
**Statuses:** active×2
**⚠ 2 account(s) pending deletion** — review the cooling-off queue

_(The 2 "pending deletion" are leftover test fixtures (`11111111-...` and `selftest`). Not real customers — safe to ignore for now. Could be cleaned up by running the deletion cron.)_

---

## What this brief tells you, in plain English

You're in the **green** on errors, **green** on engine logs, and **green** on
code activity. The one thing flagged 🟡 is **Supabase performance**: two RLS
policies on `profiles` are doing a `auth.uid()` lookup per row instead of once
per query. With ~2 active starter subscriptions today the slowdown is
invisible, but the moment you have hundreds of users + a `profiles` page-load
that fetches multiple rows, this becomes a tax. Easy fix — one SQL migration
wrapping `auth.uid()` in `(select auth.uid())` across both policies.

**Recommended next action (~15 min):** ship the perf fix via the same paste-ready
SQL flow we used for the Phase 58e security migration this morning.

_Generated by `python -m pebble.morning_brief`. Design doc: `docs/MORNING_BRIEF.md`._
