---
id: seo_check
label: Run an SEO check
description: Tighten titles, meta descriptions, headings, and alt text for search.
triggers: seo, check my seo, seo audit, search ranking, meta description, google ranking
billable: true
---
Improve this site's on-page SEO without changing its visual design or layout.

For every page:
- Ensure a unique, specific `<title>` and a compelling meta description via the App Router `metadata` export (50–160 chars).
- Exactly one `<h1>` per page, with semantic heading order (no skipped levels).
- Descriptive `alt` text on every meaningful image; empty `alt=""` only for decorative ones.
- Work the business's real location and services into headings and copy where it reads naturally.

Do NOT invent reviews, ratings, founding years, or any facts you were not given — keep placeholders in [SQUARE BRACKETS] if a value is unknown. Do NOT restructure the layout or change the design.

Output only the changed files as `<pebble-file>` blocks.
