"""Universal block library.

Each block is a (template.tsx, metadata.json) pair. These blocks are
structurally universal — a full-bleed hero, 3-col service grid, or
pull-quote testimonial works for any industry. They are described by
``vibe_tags`` (aesthetic personality) rather than a fixed industry
label, so Sonnet can pick the best-fit blocks for any brief.
"""
