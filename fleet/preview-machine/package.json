{
  "name": "pebble-preview-machine",
  "version": "1.0.0",
  "private": true,
  "comment": "Canonical Pebble-foundation dependency set, baked into the preview-machine image so `next dev` boots with no npm install. Mirrors what skills/prompt_template.md mandates for every generated site. If a build ever imports a dep not listed here, the receiver does a one-off `npm install <pkg>` delta (rare).",
  "scripts": {
    "dev": "next dev"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.0.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "gsap": "^3.12.0",
    "@gsap/react": "^2.1.0",
    "lenis": "^1.1.0",
    "framer-motion": "^11.0.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
